
import React from "react";

export const VoiceListener = () => {
  // Logic disabled as Vapi Assistant is now handling voice interactions.
  // This component is preserved for potential future custom UI needs.
  return null;
};
